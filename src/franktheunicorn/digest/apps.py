"""Django app config for the digest module."""

from django.apps import AppConfig


class DigestConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "franktheunicorn.digest"
