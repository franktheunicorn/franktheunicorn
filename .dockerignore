# Git
.git/
.github/
.gitignore

# Python
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Testing & tooling
tests/
htmlcov/
coverage.xml
.coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime data (mounted as volume)
data/

# Dev files
scripts/
# docker/ contents are COPY'd into the image (compose.*.yaml.template files
# are used by setup_llm --docker to generate local-inference overrides), so
# we only exclude the Dockerfile itself, which isn't needed at runtime.
docker/Dockerfile
docs/
*.md
!README.md
.env
.env.*
Makefile

# IDE
.vscode/
.idea/
